# Review Pass — Task acrux

**Branch:** feature/acrux
**Parent:** feature/achernar
**Spec:** metrics/task-acrux-context.md

## Checks Performed

- Read pre-computed diff (CREDITS.md, asset-manifest.ts, asset-manifest.test.ts, gen-audio-stubs.mjs, progress.md, 8 binary WAV files).
- `pnpm typecheck` — clean (3 workspaces).
- `pnpm lint` — clean.
- `pnpm test` — 51 test files / 687 tests, all pass. New asset-manifest.test.ts contributes 4 tests.
- `du -sh packages/client/public/audio` → 600K (well under 6 MB budget).
- Verified all 8 WAV files are 35–88 KB each (well above 1 KB placeholder-stub threshold). Binary asset check passes — these are real synthesized PCM WAV files, not zero-byte stubs.
- Inspected `scripts/gen-audio-stubs.mjs`: WAV header arithmetic correct (RIFF/WAVE/fmt /data chunks; subchunk sizes; ByteRate = SampleRate × Channels × BytesPerSample; BlockAlign = Channels × BytesPerSample; PCM format = 1; 16-bit signed little-endian samples clamped to ±1.0). No path-traversal risk — ROOT derived from `import.meta.url`, paths are static constants.
- Hardcoded-secrets grep: no matches (sk-, AKIA, api_key, password=).
- Accessibility: task is audio-asset bundling, no UI added — N/A.
- Cross-boundary: `AUDIO_MANIFEST: Record<AudioEvent, string>` is compile-time enforced against the `AudioEvent` union in `audio-events.ts`; runtime test enumerates keys to confirm one-to-one mapping; path patterns validated by regex (`/^\/audio\//` + `/\.(ogg|mp3|wav)$/`).
- CREDITS.md: new `## Phase 8 — Audio` section lists all 8 files with author, CC0 license, and acquired date. Format matches existing tables.
- `scripts/check-asset-licenses.sh` only walks PNGs (`walkPNGs`), so it does not validate audio files — pre-existing missing-monster-PNG failures are unrelated to this task.
- `no-console` rule applies only to `packages/*/src/**/*.{ts,tsx}`; `scripts/gen-audio-stubs.mjs` is outside that glob, so console.log usage in the stub script is permitted.
- Acceptance criteria verified:
  - All 8 audio files exist and are playable in a browser (valid PCM WAV). ✓
  - Every file documented as CC0 in CREDITS.md. ✓
  - Combined size ≤ 6 MB (600 KB). ✓
  - Manifest matches AudioEvent enum exactly. ✓
  - CREDITS.md has audio section with one row per file. ✓

## INFORMATIONAL Notes

1. **Placeholder durations vs. real-track guidance.** The spec lists 30–90 s looping themes and 1–8 s one-shots as targets for real assets; placeholder durations are 0.8–2.0 s. The spec note explicitly permits placeholder tones generated by a Node script and acceptance criteria do not constrain duration, so this is not a bug. When real CC0 tracks are swapped in, ensure their lengths fall within the spec's per-event ranges.

2. **File extension `.wav` vs. spec example `.ogg`/`.mp3`.** The "Files to create/modify" section lists `.ogg` example paths, but the spec note explicitly allows placeholder WAVs. WAV is broadly browser-supported, the manifest hard-codes the actual filenames, and the asset-manifest test accepts ogg/mp3/wav. Real-track replacements can use any of those three formats with no code changes beyond the manifest entry.

3. **Test coverage of manifest contents.** The four runtime tests (entry-per-event, no-extras, `/audio/` prefix, valid extension) plus the compile-time `Record<AudioEvent, string>` enforcement give thorough coverage. No additional tests required.

## Verdict

**PASS** — 0 bugs filed.
