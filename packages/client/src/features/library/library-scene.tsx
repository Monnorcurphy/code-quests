/**
 * Re-exports the Library modal used by the LibraryScene Phaser scene.
 * The Library scene opens this modal automatically on enter; the Bestiary
 * tab is the default selection so it is immediately discoverable.
 */
export { default } from '../library';
