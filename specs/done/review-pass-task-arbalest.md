# Review Pass — TASK arbalest (Quest scene art assets + asset-loader keys)

## Checks performed

- Read task spec (`metrics/task-arbalest-context.md`) and validated against diff.
- Read `packages/client/src/game/asset-loader.ts` — new `QUEST_ASSET_KEYS`, `preloadQuestAssets`, union `AssetKey` type all look correct.
- Read `packages/client/src/game/__tests__/asset-loader.test.ts` — 7 new tests cover existence, ≥1 KB floor, ≤500 KB ceiling, `assetPath()` round-trip, `preloadQuestAssets` registration coverage, non-pollution of common keys, RGBA PNG signature + color-type 6 verification for silhouettes.
- Read `scripts/gen-asset-stubs.mjs` — new `makePNGRGBA` builder (color type 6), 5 new palettes (FOREST/CAVE/DUNGEON_QUEST/BOSS/SILHOUETTE), 2 humanoid shape functions, 14 new entries in the `ASSETS` / `RGBA_ASSETS` arrays. Generator's own min-1KB/max-500KB checks remain.
- Read `assets/CREDITS.md` Phase 5 section — every new file has source URL, creator, license, and attribution text (CC-BY for 0x72 boss room is recorded with required attribution).
- Verified disk artifacts: 14 PNGs in `assets/quest/`, all between 1.9 KB and 35 KB, well inside the 1–500 KB band.
- Verified symlink: `packages/client/public/assets` → `../../../assets` already exists, so Vite sees the new files (confirmed by build output below).
- Ran `pnpm --filter @code-quests/client test` → 331/331 pass (28 files).
- Ran `pnpm --filter @code-quests/client typecheck` → clean.
- Ran `pnpm --filter @code-quests/client lint` → clean.
- Ran `pnpm --filter @code-quests/client build` → succeeded, all 14 quest PNGs copied to `dist/assets/quest/`.
- Ran `node scripts/check-asset-licenses.mjs` → **FAILED** with 14 `MISSING FROM MANIFEST` violations (see review-1.md).
- Grepped diff for `sk-`, `AKIA`, `api_key`, `password=` → none.
- Confirmed cross-boundary surface is asset-path only: `assetPath()` strings line up with the symlinked URL space (`/assets/quest/...`).
- Confirmed accessibility scope is unchanged (no UI added).
- Confirmed the `AssetKey` union still type-checks both branches (no `any`).

## Bugs filed

| # | Severity | Title |
|---|----------|-------|
| 1 | CRITICAL | New quest PNGs are missing from assets/manifest.json — `pnpm check:assets` fails |
| 2 | LOW      | Duplicate `---` separator at the top of the Phase 5 CREDITS section |

## INFORMATIONAL notes

- **Stub size vs. spec target.** Task spec says background images should be "≥ 2400×720" parallax-ready. Current stubs are 256×64 — explicitly documented in `assets/CREDITS.md` ("Phase 5 background stubs are 256×64 px; real assets must be ≥ 2400×720 px") and gated by the existing "synthetic stubs generated by scripts/gen-asset-stubs.mjs — replace before release" workflow. This matches the project-wide stub policy; not a bug, but the eventual capstone for Phase 5 must verify real assets are downloaded before release.
- **Pack name "Tiny Dungeon (extended)".** The CREDITS entry that maps cave + dungeon quest backgrounds/props to "Kenney Tiny Dungeon (extended)" is slightly fictional — the real Kenney Tiny Dungeon pack doesn't ship parallax backgrounds. Once real artwork replaces the stubs, this attribution line will likely need to be split (e.g., cave backgrounds may end up sourced from a different Kenney pack such as Toon Characters / Platformer Art Caves). Acceptable while assets are stubs; flag for the asset-sourcing task.
- **No in-app credits screen yet.** CREDITS.md notes that CC-BY attribution "must appear in the app's credits screen" — that screen does not exist yet. Out of scope for this task (asset bring-up), and tracked implicitly by Phase 5's Hall of Returns / credits work.
- **`Buffer` in test file.** The new asset-loader test uses Node's global `Buffer` without importing `@types/node`. Typecheck passes because vitest's runtime types pull it in transitively; no action needed, just noting for future test reorganization.

## Verdict

**FAIL** — 2 bugs filed (1 CRITICAL, 1 LOW). The CRITICAL must be fixed before merge because it breaks `pnpm check:assets`.
